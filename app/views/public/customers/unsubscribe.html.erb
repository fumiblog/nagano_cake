<div class="container">
    <div class="function_taikai">
      <%= form_with model: [:public @customer], local: true do |f| %> 
      <h2>本当に退会しますか？</h2>
      <t>退会すると、会員登録情報や</t></br>
      <t>これまでの購入履歴が閲覧できなくなります。</t></br>
      <t>退会する場合は、「退会する」ボタンをクリックして下さい。</t>

  <%= link_to "退会しない", edit_public_customer_path(@customer.id), class:"btn btn-primary"%></div>
  <%= link_to "退会する", edit_public_customer_path(@customer.id), class:"btn btn-danger" %>
  
  <% end %>

</div>